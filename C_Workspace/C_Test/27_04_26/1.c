//WAP to print HELLO WORLD With out using semocoloun//
#include <stdio.h>

int main() {
    // printf returns the total number of characters it printed.
    // In C, any non-zero number is treated as "TRUE".
    if(printf("Hello World"))
    {
        // Since "HEllo World" is 10 characters long, 
        // printf returns 10. 10 is not 0, so the 'if' condition is true.
        
        // This block is empty, but it still executes!
    }

    return 0;
}
